# config.py

DATABASE_CONFIG = {
    "host": "localhost",            # The hostname or IP address of the database
    "port": 3306,                   # The port number for the database (default for PostgreSQL)
    "user": "admin",                # The username to connect to the database
    "password": "admin",            # The password for the user
    "dbname": "MemeUsers"           # The database name
}
